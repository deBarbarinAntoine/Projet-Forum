{{define "tag-update"}}
 <div class="blur-underlay"></div>

    <div class="center-page">
        <div class="container borders">
            <form class="login">
                <p class="login-title bottom-outset"> Update Tag </p>
                
                <div class="input-fields">
                    <label for="title"></label>
                    <input class="input borders" type="text" placeholder="Update tag name" id="tagname">


                </div>
                
                <button type="submit" class="switch borders"> Change </button>
            </form>
    
        </div>
    </div>
    {{end}}